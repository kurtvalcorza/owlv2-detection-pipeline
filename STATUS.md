# Release status

Current status: **Candidate — E2E source/static checks pending final review; model-backed execution deferred to Kaggle Tesla T4**. The package carries bounded class-head and box-head adaptation, labelled-box validation and split helpers, corpus metrics and baselines, SafeTensors adapter export/reload, and the standalone `E2E` notebook `tutorials/owlv2_detection_colab.ipynb` (DIMER Notebook Specification 2.0 §4). Offline tests, the release-asset validator, and generator parity are necessary but are not model-backed or supported-runtime evidence. The earlier `TASK-INFERENCE` blob's Kaggle CPU PASS remains historical only; this E2E blob is not release-grade until its exact commit passes the serial Kaggle Tesla T4 suite and the result is recorded in `docs/release-verification.md`.

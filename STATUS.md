# Release status

Current status: **Release-grade — exact-blob E2E run passed on Kaggle Tesla T4** (commit `a772a31`, notebook blob `c9cd132564ae`, `kurtvalcorza/dimer-nb2-owlv2-detection` v2, 2026-09-25 UTC, 705.1 s, 11/11 post-restart code cells). The package carries bounded class-head and box-head adaptation, labelled-box validation and split helpers, corpus metrics and baselines, SafeTensors adapter export/reload, and the standalone `E2E` notebook `tutorials/owlv2_detection_colab.ipynb` (DIMER Notebook Specification 2.0 §4). On the default BCCD sample (one seeded 260 / 40 / 64 split, 64 held-out records, 846 reference boxes) the adapted heads measured test mAP50 0.8918 against 0.0723 for the frozen model and 0.0147 for the grid prior, with fresh-base reload parity 8/8; the full record, runtime identity and output digests are in `docs/release-verification.md`. These are measurements of one split on one runtime, not a benchmark. Any change to the notebook blob returns the carrier to Candidate until a new exact-blob run is recorded. The earlier `TASK-INFERENCE` blob's Kaggle CPU PASS remains historical only.

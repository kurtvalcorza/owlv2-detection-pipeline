# Tutorials

[![GitHub](https://img.shields.io/badge/GitHub-181717?style=flat&logo=github&logoColor=white)](https://github.com/kurtvalcorza/owlv2-detection-pipeline)
[![Open In Colab](https://colab.research.google.com/assets/colab-badge.svg)](https://colab.research.google.com/github/kurtvalcorza/owlv2-detection-pipeline/blob/main/tutorials/owlv2_detection_colab.ipynb)
[![Hugging Face](https://img.shields.io/badge/%F0%9F%A4%97%20Hugging%20Face-google%2Fowlv2--base--patch16--ensemble-ffcc4d?style=flat)](https://huggingface.co/google/owlv2-base-patch16-ensemble)
[![Upstream](https://img.shields.io/badge/Upstream-google--research%2Fscenic%20(owl__vit)-181717?style=flat&logo=github&logoColor=white)](https://github.com/google-research/scenic/tree/main/scenic/projects/owl_vit)
[![arXiv](https://img.shields.io/badge/arXiv-2306.09683-b31b1b.svg)](https://arxiv.org/abs/2306.09683)

Notebook specification: **DIMER Notebook Specification 2.0** — the notebook is **standalone** (§4) and declares its profile and pedagogical mode (§3.4): it is
generated by `tools/build_notebook.py` from `tools/notebook_template.py` and carries the repository's
pipeline module, model identity, snapshot manifest and runtime pins, so the exported `.ipynb` keeps working
without this repository being reachable. Do not edit the notebook by hand; edit the package or the template
and regenerate (`python tools/build_notebook.py`; `--check` is enforced by the validator and CI).

| Notebook | Profile | Mode | Carrier | Capability | Default runtime | Sample | BYOD | Run-all | Release status |
|---|---|---|---|---|---|---|---|---|---|
| `owlv2_detection_colab.ipynb` | `TASK-INFERENCE` | `GUIDED` | standalone (generated) | zero-shot text-prompted object detection with `google/owlv2-base-patch16-ensemble` (CLIP ViT-B/16, 960×960, 3,600 patch candidates, no NMS); score-ordered xyxy boxes labelled with the best-matching query and the model's sigmoid score (uncalibrated for the deployment); caller-owned `threshold` exposed as a form parameter; `validate_inputs` → input manifest; `evaluation_report` → `sample-sanity` with per-object `box_iou` only when reference boxes are supplied, otherwise `not-measurable`; no mAP | CPU (CUDA used automatically when available) | automatic (640×480 scene drawn in code: black rectangle, red disc, blue triangle; three prompts) | single image (16–4096 px per side) plus comma-separated prompts, gated off by default; no reference boxes, so the report is `not-measurable` | verified — clean-room Kaggle CPU execution recorded in `../docs/release-verification.md` (all 8 code cells, 68.6 s, snapshot staged from the Hub); no Colab/Kaggle run yet (REL1) | **Candidate** — static checks pass and a local fresh-kernel execution is recorded; clean-room Kaggle execution recorded in `../docs/release-verification.md` |

## Conformance notes

- **Standalone carrier (NOTEBOOK_SPEC 2.0 §4):** the primary path performs no clone, repository install, or repository import. Section 2 is `src/owlv2_detection_pipeline/pipeline.py` verbatim (tagged `metadata.dimer.embedded_module`; the only rewrite makes the default weights directory working-directory-relative), Section 3 carries the model identity and the 9-file SHA-256 manifest inline and asserts they agree with the module before anything is fetched, and Section 1 carries the exact `pyproject.toml` runtime pins. `metadata.dimer.generated_from` records the repository, the generating revision, the module's SHA-256, and the generator version.
- **Parity (PAR1–PAR3):** `tests/test_notebook_parity.py` and `tools/validate_release_assets.py` fail when the carried module, the inline manifest, or the inline pins differ from the repository, or when the notebook is not byte-identical to the generator's output.
- **Model acquisition (MOD1–MOD8):** `stage_missing_files(WEIGHTS_DIR, allow_download=True)` fetches only the manifest entries that are absent, from the Hugging Face Hub at the immutable revision (never `main`); `verify_snapshot` re-hashes every entry; `from_pretrained(weights_dir=WEIGHTS_DIR)` loads the verified files with `local_files_only=True` and `trust_remote_code=False` through `Owlv2ForObjectDetection`/`Owlv2Processor`. The upstream `pytorch_model.bin` is not listed and not used. The notebook's only external dependencies are the pinned PyPI distributions and the Hub at that revision.
- **Role stages:** `validate_inputs` writes `outputs/owlv2_detection_input_manifest.json` with the schema (prompt ceilings, the 16-token CLIP limit, the 3,600-candidate ceiling, the square padding), the ceilings, the input's observed mode and size, the normalised queries, the threshold, the verdict, and one recorded rejection finding from the over-long-prompt probe; `evaluation_report` writes `outputs/owlv2_detection_evaluation_report.json` — `sample-sanity` with one `box_iou` entry per drawn reference box (value, best-matching detection's label, whether the label agrees) on the synthetic scene; `not-measurable` on BYOD (with what labelled data would make the task measurable).
- The default sample is a deterministic 640×480 scene drawn in code with Pillow (a black rectangle, a red disc, a blue triangle on grey) with three prompts naming the shapes — the same scene and prompts the smoke run used; `box_iou` against shapes you drew yourself is sanity evidence for the input contract, query formatting, forward pass and coordinate mapping (including the square padding), not a detection metric. The scene has no text rendering, so its digest is stable across Pillow builds; it is printed, not asserted.
- Score semantics (UNC1–UNC4): every `score` is a sigmoid of the best image–text logit for a patch, not a calibrated probability and not exclusive across queries; the notebook states that there is no non-maximum suppression and that duplicates appear at low thresholds. The threshold is a caller-owned request parameter exposed as a `# @param` value with the package default 0.1 (the README example's value, not a calibration) and passed explicitly on every `detect` call. The notebook performs no validation of its own — image, prompts and threshold are checked only by the pipeline's `validate_inputs`/`detect` path.
- Ceilings `MIN_IMAGE_SIDE` (16), `MAX_IMAGE_SIDE` (4096), `MAX_PROMPTS` (16), `MAX_PROMPT_CHARS` (48), `MAX_TEXT_TOKENS` (16), `MAX_DETECTIONS` (3600) and `DETECTION_THRESHOLD` are printed before the model runs.
- CPU is documented as adequate (card-measured 5.3 s load, 2.9–3.4 s per `detect` on the 640×480 scene; 4.4 s on a 4096×4096 blank image — which returned no boxes at the default threshold, unlike the closed-set RT-DETR sibling).
- `USE_BYOD` defaults to `False` so the sample path never opens an upload dialog.
- `tools/validate_release_assets.py` performs source validation only. It does not satisfy the
  clean-runtime execution requirement; a release review must confirm that a recorded clean run in
  `docs/release-verification.md` matches the notebook revision under review before the status is
  promoted to `Release-grade`.

## AI Assistance Disclosure

This tutorial and its accompanying documentation were developed with generative AI assistance for code development and technical writing under maintainer direction. The maintainer remains responsible for reviewing the implementation, validating results, and making release decisions. AI assistance does not constitute independent verification, provider endorsement, or release approval.
